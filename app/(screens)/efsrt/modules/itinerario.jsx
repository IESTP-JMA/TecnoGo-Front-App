import { View, Text, TouchableOpacity, FlatList } from "react-native";
import { useRouter } from "expo-router";

export default function DocumentRequestsList() {
  const router = useRouter();

  return (
    <View>
      <Text>In Itenario.jsx</Text>
    </View>
  );
}
